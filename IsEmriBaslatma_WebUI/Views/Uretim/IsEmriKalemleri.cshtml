@model List<IS_EMRI_KALEMLERI>
@{
    ViewBag.Title = "IsEmriKalemleri";
}

<div class="col-xl-12 col-md-12 col-sm-12 col-12  ">
    <div class=" ">
        <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                <!-- İş Emri Bilgileri -->
                @*@Html.Partial("_isEmriBilgileri")*@
                <!-- İş Emri Bilgileri -->
                <!-- Tablo -->
                @Html.Partial("_isEmirleriDetayTablo")
            </div>
        </div>
    </div>
</div>
<form class="needs-validation" method="post" action="/Uretim/urunDetaylariPopup">
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="myModalLabel">Veri Detayları</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="modalBodyContent">
                    <!-- Dinamik veri buraya yüklenecek -->
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-secondary" data-dismiss="modal">Kaydet</button>
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Kapat</button>
                </div>
            </div>
        </div>
    </div>
</form>
@*@Html.Partial("_urunDetaylariPopup")*@
